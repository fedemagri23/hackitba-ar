import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['*.ngrok-free.app', 'your-custom-domain.ngrok.io', 'unmined-peyton-amphitheatrically.ngrok-free.dev'],
};

export default nextConfig;
